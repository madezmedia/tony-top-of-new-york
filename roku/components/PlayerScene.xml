<?xml version="1.0" encoding="utf-8" ?>
<component name="PlayerScene" extends="Group">
  <script type="text/brightscript" uri="pkg:/components/PlayerScene.brs" />

  <interface>
    <field id="episodeId" type="string" onChange="onEpisodeIdChange" />
    <field id="episodeTitle" type="string" />
    <field id="streamUrl" type="string" />
  </interface>

  <children>
    <!-- Solid black background to cover home screen -->
    <Rectangle
      id="background"
      width="1920"
      height="1080"
      color="#000000"
    />

    <!-- Video player — use enableUI=true for built-in transport controls -->
    <Video
      id="videoPlayer"
      width="1920"
      height="1080"
      translation="[0, 0]"
      enableUI="true"
    />

    <BusySpinner
      id="loadingSpinner"
      translation="[930, 480]"
      visible="true"
    />

    <Label
      id="errorLabel"
      text=""
      color="#FF4444"
      font="font:MediumSystemFont"
      translation="[80, 500]"
      visible="false"
    />

    <!-- Back hint overlay -->
    <Label
      id="backHint"
      text="◀ Back"
      color="#AAAAAA"
      font="font:SmallSystemFont"
      translation="[40, 30]"
    />
  </children>
</component>
